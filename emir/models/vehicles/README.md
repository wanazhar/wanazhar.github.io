# Vehicle GLB placeholders

These tiny `.glb` files are deliberately simple voxel placeholders. Replace them with proper art exports when ready.

Required node names:

- `chassis`
- `wheel_fl`
- `wheel_fr`
- `wheel_rl`
- `wheel_rr`

The runtime decouples those wheel nodes for steering, spin and suspension travel.
